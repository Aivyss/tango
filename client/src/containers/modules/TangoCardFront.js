import {connect} from 'react-redux';
import TangoCardFront from '../../components/modules/TangoCardFront';

function mapStateToProps(state) {
    return {};
}

function mapDispatchToProps(dispatch) {
    return {};
}

export default connect(mapStateToProps, mapDispatchToProps)(TangoCardFront);
